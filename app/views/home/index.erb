<%= render 'shared/app_top' %>
<div class="hero-unit">

<h2>A list of your documents</h2>
<% current_user.assets.each do |ca| %>
<div>
	<%= ca.file_name %>
</div>
<% end %> 

	
</div>

<%= render 'shared/app_bottom' %>